/** Distances use one hex radius; locomotion stays at real-time speed while industry is paused/accelerated. */
export const EXPLORER = Object.freeze({ stepMs: 20, speed: 3.2, sprintSpeed: 5.6, radius: 0.18, buildRange: 6 });
