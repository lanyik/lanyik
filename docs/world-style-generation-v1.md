# 世界风格生成 v1 设计

状态：世界风格 v1 阶段 0–15 已完成；2026-09-05 将河长改为支持超过基准的真实上游增长，保留展开小地图原位调参刷新，当前冻结生成器为 v19。

本文记录已冻结的世界生成与地形表现方案。目标不是模拟真实地球，而是在保留六边格、区块流式加载、工作线程和现有资源预算的前提下，生成风格统一、自然、耐看的世界。

基础设施约束见 [foundation-v1-freeze.md](./foundation-v1-freeze.md)，测试规则见 [testing.md](./testing.md)。

## 1. 结论

采用三层结构：

~~~text
世界描述符 + 当前生成器版本
              │
              ▼
      世界地表解析器
 WorldSurfaceResolver
              │
       ┌──────┴──────┐
       ▼             ▼
 一次性生成       区块工作线程
 generateWorld    generateWorldChunk
       │             │
       └──────┬──────┘
              ▼
       MapInfo / PackedWorldChunk
              │
              ▼
       生效地表视图
      WorldSurfaceView
  生成基础 + 稀疏编辑 + 显示尺度
              │
              ▼
 地形、城市、单位、植被、标记、自定义层
~~~

核心决定：

- 保留逐格六边形地形，不改成连续地形块或 clipmap。
- 保留 WorldSource、源区块、12×12 渲染块、LOD、生命周期和资源预算。
- 生成规则只保留一份，由 WorldSurfaceResolver 同时服务一次性生成和工作线程。
- 渲染器始终读取“生效地形”，不能用种子生成结果覆盖玩家编辑。
- 区块构建时使用短命的 WorldSurfaceWindow 去重采样，不建立全世界缓存。
- v1 只有一种内部风格配置，由 WORLD_GENERATOR_VERSION 锁定，不增加多余的风格版本字段。
- 512×512 是主要有限世界样本，但算法不能写死 512。
- 不建设物理水循环或全局地形图；水体只实现确定性的宏观海域和有界粗网格排水网络。

## 2. 目标与非目标

### 2.1 目标

1. 多个种子都能产生连贯的大陆、海域、汇流河网、山系、平原和森林。
2. 山体内部连续，边缘有山麓，不出现大量单格尖峰。
3. 森林形成区域；海域不受地表细节噪声打散，河道不呈现椒盐状随机点。
4. 相同描述符和坐标的结果不受区块大小、请求顺序、工作线程数量和缓存命中影响。
5. 源区块、渲染块、LOD、环绕边界、卸载重载和浮动原点没有可见接缝。
6. 城市、单位、路线、草木和标记能正确贴合地表。
7. 六边格边界、选择反馈和玩法可读性仍然清楚。
8. 新增成本继续受现有工作队列和资源预算约束。

### 2.2 非目标

- 不模拟板块运动、真实侵蚀和全球水循环。
- 不把地形渲染改成与六边格无关的连续网格。
- 不重写流式加载、区块驻留、LOD 调度和渲染层生命周期。
- 不开放几十个公共生成参数。
- 不保证新生成器与旧种子生成相同世界。
- 不提前建设通用地形特征系统、全局地图集或 AssetRegistry。
- 不以噪声复杂度和单测数量衡量视觉质量。

## 3. 当前实现的主要问题

现有基础可继续使用：

- 设计起点的 WORLD_GENERATOR_VERSION 为 3；连续地表、森林和雪线修订依次推进到 6；海域和河网修订推进到 12，河道自然度复核推进到 13，整格河岸栅格修订推进到 14，河源延伸与渐宽河口推进到 15，曲线中心线推进到 16。
- LandformSampler 提供 elevation、continentalness、ridge、valley、roughness、moisture、temperature 和独立低频 ocean 场。
- 无限世界使用全局坐标；环绕世界使用周期噪声；有界世界有边缘衰减。
- PackedWorldChunk 使用 16 位格子数据，并带一圈邻域。
- TerrainMesh 已使用六邻居高度构造共享山体顶角。
- Shader 的细节相位来自逻辑世界坐标，不受区块顺序和浮动原点影响。

需要解决的问题：

1. generateWorld.ts 与 generateWorldChunk.ts 重复实现地形分类、湖泊、丘陵、森林和树种规则。
2. 生成器与渲染器只共享原始地貌场，没有唯一的最终地表规则。
3. 普通陆地、丘陵和谷地仍缺少统一的连续高度。
4. 森林和湖泊仍带有较强的逐格随机感。
5. 静态 MapInfo 没有种子，只能使用中性山体高度。
6. 现有测试只证明数据邻域一致，没有直接证明最终几何跨区块和 LOD 无缝。
7. 当前 Shader 会用较宽的系数缩放整座山体，这已经不是“小细节”，CPU 无法据此准确贴地。
8. Terrain 当前使用 15 个顶点属性位置，新增地貌字段容易超过部分 GPU 的能力。
9. 地形块的 Y 方向裁剪边界是经验值，扩大起伏后可能提前消失。
10. 缓存键、默认 worldId 和导航 terrainRevision 分别拼接身份字段，容易出现漏项。

## 4. 核心结构

### 4.1 生成地表

LandformSampler 只负责连续基础场。

WorldSurfaceResolver 负责把基础场解释成地形、地表高度、气候、植被、海域和排水河道：

~~~ts
interface WorldSurfaceResolver {
    readonly seed: string;
    readonly domain: LandformDomain;
    readonly profile: WorldStyleProfile;
    readonly waterStyle: WorldWaterGenerationStyle;

    sampleGenerated(x: number, y: number): Readonly<WorldSurfaceSample>;
    resolveGeneratedTile(x: number, y: number): Readonly<TileInfo>;
    visitGeneratedRiverTiles(originX, originY, width, height, visit): void;
    createWindow(): WorldSurfaceResolverWindow;
}
~~~

sampleGenerated() 只处理单点连续结果，不逐像素追踪河道。resolveGeneratedTile() 合并分页河网并读取固定的一圈六邻域，用于海岸和最终格子修饰。visitGeneratedRiverTiles() 一次追踪并栅格化目标范围内的河道，供小地图避免逐格解析整个世界。

这两个接口首版保持包内使用，不直接成为公共 API。

### 4.2 地表样本

~~~ts
type WorldBiome =
    | "ocean"
    | "coast"
    | "temperate"
    | "dry"
    | "cold"
    | "alpine";

interface WorldBiomeWeights {
    readonly temperate: number;
    readonly dry: number;
    readonly cold: number;
    readonly alpine: number;
}

interface WorldSurfaceSample {
    readonly baseTerrain: Land;
    readonly relief: number;
    readonly biome: WorldBiome;
    readonly biomeWeights: WorldBiomeWeights;
    readonly vegetationDensity: number;
    readonly vegetationKind?: "palm" | "pinia" | "oak";
    readonly landform: Readonly<LandformSample>;
}
~~~

说明：

- relief 是归一化的生成高度，不是最终世界 Y。
- landform 保留生成器 v3 的原始连续场，只供渲染调试和后续风格阶段复用。
- biome 是主标签，主要用于统计和树种选择。
- biomeWeights 用于连续材质混合，不能直接按 biome 画硬边界。
- vegetationDensity 是连续值，不是最终布尔结果；宏观海域由 landform.ocean 与固定阈值分类。

### 4.3 生效地表

运行时权威结果仍然是：

~~~text
生成基础格子 + WorldTileOverride = 生效格子
~~~

WorldSurfaceView 负责把当前 MapInfo、可选解析器和显示尺度合并起来：

~~~ts
interface WorldSurfaceAnchor {
    readonly revision: number;
    readonly minimumHeight: number;
    readonly maximumHeight: number;
    getTileCenterHeight(x: number, y: number): number;
    getWorldHeight(worldX: number, worldZ: number): number;
}

interface WorldSurfaceView extends WorldSurfaceAnchor {
    readonly map: MapInfo;
    readonly resolver?: WorldSurfaceResolver;
    readonly tileSize: number;
    readonly mountainHeight: number;
    getEffectiveRelief(x: number, y: number): number;
    createWindow(): WorldSurfaceWindow;
    setMountainHeight(value: number): boolean;
}
~~~

WorldSurfaceView 不拥有格子，也不写入存档。创建时必须校验地图拓扑、尺寸与解析器一致。

### 4.4 编辑后的高度规则

所有内置渲染层使用同一优先级：

1. 当前没有该格子：返回岸线基准高度。
2. 生效类型为 sea、coastal，或包含 lake：返回岸线基准高度。
3. 生效类型为 mountain：使用山体高度范围，并保证最小山高。
4. 包含 hill：使用丘陵高度范围，并保证最小丘陵高度。
5. 其他陆地：把生成高度限制在可通行地形范围，移除 mountain 后不会留下高台。
6. 静态地图没有解析器时：mountain 使用当前中性山高，其他类型保持平地。

type 或 modifiers 编辑只改变所属高度范围，不代表自由修改高度。真正的造山、挖坑和连续高度编辑以后必须使用单独版本化的 SurfaceDelta。

### 4.5 临时采样窗口

WorldSurfaceResolverWindow 和 WorldSurfaceWindow 都只用于一次生成块或渲染块构建：前者缓存生成样本与最终生成格子，后者缓存当前 MapInfo 合并稀疏编辑后的地表贡献、共享顶角和高度查询。

- 缓存核心格子、输出邻域和计算需要的有限外圈。
- 环绕世界先规范化坐标再作为缓存键。
- 同一坐标在一次构建中只采样一次。
- 构建完成立即释放。
- 结果必须与逐坐标调用解析器完全一致。

它不是新的存储格式，也不能变成无界的全世界缓存。

### 4.6 文件边界

| 文件 | 职责 |
|---|---|
| src/world/WorldGeneratorVersion.ts | 生成器版本与版本校验；原导出路径继续转出 |
| src/world/WorldStyleProfile.ts | 冻结基础风格、水体生成参数的规范化和校验 |
| src/world/WorldSurfaceResolver.ts | 唯一生成规则 |
| src/world/WorldWaterSampler.ts | 粗网格河源、下坡追踪、汇流宽度、分页缓存与环绕掩码 |
| src/world/generateWorldOverview.ts | 低分辨率地表采样与一次性河道覆盖栅格 |
| src/world/WorldSurfaceView.ts | 生效格子、显示尺度和地表高度查询 |
| 解析器和视图内部辅助代码 | 短命 WorldSurfaceResolverWindow / WorldSurfaceWindow |

依赖方向固定为：

~~~text
风格配置 + LandformSampler
            ↓
 WorldSurfaceResolver
            ↓
   WorldSurfaceView
            ↓
         渲染层
~~~

src/world 不得导入 Three.js、HexMap 或 rendering 模块。

只把 WORLD_GENERATOR_VERSION 移到中性小文件，避免 generateWorldChunk → resolver → profile → generateWorldChunk 循环依赖。其他协议版本不做无关移动。

## 5. 风格配置与确定性

### 5.1 WorldStyleProfile

完整风格配置保持内部只读，按以下几组组织：

| 分组 | 内容 |
|---|---|
| fields | 扭曲、大陆、细节、山脊、谷地、粗糙度、湿度、温度、森林斑块和独立低频海域尺度 |
| terrain | 海域阈值、地表起伏基准、山地/丘陵阈值、雪地/冻土/沙地阈值、气候过渡宽度 |
| relief | 平地幅度、谷地深度、丘陵/山麓/山体高度范围和山体曲线 |
| vegetation | 湿度阈值、最大密度、斑块强度、坡度惩罚、区域放置阈值/边缘抖动和树种温度范围 |
| rivers | 分页上限、粗采样间距、河源密度、河源适宜度、下坡势能权重和最大路径长度 |

规则：

- `WORLD_STYLE_PROFILE` 是由生成器版本确定的冻结基础配置。
- 应用只可覆盖 `WorldWaterGenerationStyle`：海域频率/阈值，以及河源密度、弯曲和汇流宽度；其余内部系数不开放。
- 水体参数在数据源构造时完成严格校验和冻结，并写入 `WorldDescriptor`，不读取运行中的全局可变状态。
- 修改基础生成语义时，必须升级生成器版本并更新格子、地表和配置基准；只调整某个世界的合法水体参数不升级生成器版本。
- field scale 必须真正传入 LandformSampler，不能再保留另一套隐藏常量。
- 海域频率同时缩放 open/toroidal 采样频率和 toroidal 最小周期数，避免小环形世界被最小周期钳制后滑块失效。

现有公开 createLandformSampler({ seed, domain }) 仍表示“当前生成器的默认地貌场”。内部解析器通过未导出的工厂传入冻结配置。

### 5.2 确定性规则

- 所有随机选择使用坐标与 salt 的稳定哈希；普通逐格选择继续使用 randomAt(seed, x, y, salt)。
- 不使用共享随机游标。
- 河源、排水势能、植被和树种等用途使用不同且稳定的 salt。
- 格子坐标必须是安全整数。
- 区块大小、工作线程编号、请求时机、镜头和缓存状态不能参与生成。
- 环绕世界先规范化坐标。
- 所有阈值输入先规范化并限制范围。
- 如果将来需要定点量化，必须在新生成器版本中统一应用于一次性生成、工作线程和测试。

## 6. 地形生成规则

### 6.1 基础分类

1. 独立低频 ocean 场低于海域阈值：sea；terrain detail、ridge 和 roughness 不参与海陆掩码。
2. elevation 足够高且 ridge 足够强，或 elevation 达到极高保底值：mountain。
3. 其余陆地先按 temperature 分 tundra；snow 还必须越过随低温下降、但不会落到岸线的气候雪线。
4. 高温低湿分 sand。
5. 剩余为 land。
6. coastal 不是基础类型；最终 sea（包括河道）的六邻居存在陆地时才派生。

generateWorld()、generateToroidalWorldTile() 和 generateWorldChunk() 必须删除各自的规则副本，统一调用解析器。

### 6.2 地表起伏

视觉层次为：

~~~text
平地和谷地 < 丘陵 < 山麓 < 山体
~~~

约束：

- 海面和湖泊使用统一岸线基准。
- 平地只允许很小的起伏。
- 谷地可以轻微下沉，但不能越过河湖职责边界。
- 丘陵由连续 elevation 区间决定。
- 山体高度主要由 elevation 决定，ridge 用于塑造山脊，不直接等于高度。
- 普通可通行地形的起伏必须明显低于山体。
- relief 不改变通行性和移动消耗；玩法继续读取生效 TileInfo。

mountainHeight 仍是显示尺度，不进入生成身份。修改它时，地形、地表查询和贴地对象必须一起刷新。

### 6.3 共享顶角

一个世界顶角由三个格子共享：

~~~text
cornerHeight =
    combine((kindA, reliefA), (kindB, reliefB), (kindC, reliefC))
~~~

combine 必须满足：

- 对三个输入完全对称。
- 不依赖“当前正在绘制哪个格子”。
- 相邻渲染块和不同 LOD 得到相同边界高度。
- 格子中心高度由六个共享顶角推导，不能再次强制成格子自身峰值。

光照法线也使用共享顶角契约：每个世界顶角只由相交的三个格子中心 relief
解出同一坡度，相邻实例在公共边上插值相同的两个端点坡度。不得再从单格局部
三角扇向外有限差分，否则几何虽连续，光照仍会勾出六边形接缝。

getTileCenterHeight() 必须复用同一套六顶角结果。getWorldHeight() 在同一 CPU 三角扇上插值。

首版岸线规则：三个贡献格子中只要有 sea、coastal、lake 或地图外空位，该顶角就落到岸线基准。若样本图库证明临海山体被明显削平，再改成同样对称且有限范围的岸线衰减。

### 6.4 Shader 细节

CPU 三角扇拥有山体、山脊和峰高。Shader 只允许增加有界的小位移、法线和材质细节。

阶段 4 已把 mountainHeightAt() 从最高 1.57 倍的宽乘法收紧为宏观地表的 ±1.5% 微位移，并把每次高度计算的细节噪声从四次采样减为两次。默认 mountainHeight 为 80 世界单位，因此最高归一化 relief 处的 CPU/GPU 偏差不超过 1.5 世界单位；可放置城市、单位或植被的区域不会再出现肉眼明显的悬空。

细节还必须满足：

- 只使用逻辑世界坐标。
- 共享边界同一点得到相同结果。
- 不改变地形类型、通行性、气候和存档语义。
- full 与 fast 模式保持相同宏观表面和边界。

### 6.5 气候与植被

biome 由 elevation、temperature 和 moisture 的宽过渡带得到。

植被密度：

~~~text
气候适宜度
× 连续森林斑块场
× 坡度惩罚
~~~

generator v5 不再用独立坐标随机值直接与密度比较。最终 wood 由连续密度跨过固定区域阈值得到，坐标随机值只提供幅度受限的边缘抖动；零密度格不能因抖动进入森林。这样保留确定性和自然边缘，同时避免低适宜度区域出现椒盐状单格树。树种主要由气候决定。

编辑优先级：

- 生效格子没有 wood：密度为 0。
- 手工增加 wood：至少使用中性林区密度。
- treeModel 编辑优先于生成树种。

### 6.6 海域与排水河网

旧规则直接用包含高频 detail、ridge 和 valley 的最终 elevation 切海平面，放大到 2048×2048 时会产生大量不相连的噪声水点。当前规则把职责拆开：

- ocean 是不经地形扭曲的低频连续场，只负责海陆掩码；有界世界仍叠加中心抬升和边缘衰减，保证中央陆地与外围海域。
- 河道在间距为 8 个世界六边格的粗轴向网格上工作，不在 2048² 个格子上做全图流向计算；节点在采样和栅格化前叠加连续扭曲，默认频率 0.08、幅度上限 3.75 格，避免主世界暴露规则的六方向折线。幅度不再被内部静默截为 3.5；小型环绕世界若缩小排水步长，扭曲幅度同比缩小，保持小于实际步长一半的约束。
- 稳定河源只从中高海拔的非水、非山雪格产生；每个固定源区提出 4 个候选，不随请求顺序变化。
- 每一步只选择排水势能严格下降的六邻居。势能以 ocean 为主，并由 elevation、valley、moisture 和极小确定性抖动修正。
- 只保留达到宏观海域且满足最短长度的路径。种子河源作为长度基准锚点，沿真实入流图寻找累计渲染中心距离最长的上游支路，替换原来“最高相邻势能 + 至多三步”的贪心延伸。源头到海总节点数仍不超过 72；普通陆地不必满足独立河源的气候/海拔门槛，但山雪、缺失域和分水岭会终止延伸。候选的唯一下游必须指向当前节点，逆流势能严格递增，不会环绕成圈。按规范坐标与剩余节点预算缓存最优支路，缓存只存活于单次范围计算。失败的内陆路线仍不落盘，不生成孤立湖点。
- 下游选择只依赖坐标连续场，因此两条路径到达同一粗格后自然共享后续路径；重叠河源数作为汇流量。半径在默认支流 1.75 与主河 4 之间按 smoothstep(1, 24, flow) 连续过渡，不再突然跳档。
- 沿渲染世界空间中心线反向累加至入海终点的距离，在最后 24 格的邻格间距内以 smoothstep 渐宽，到海时为当前流量半径的 1.6 倍；远离河口的河宽不受影响，不能用欧氏临海距离把邻近海湾但尚未入海的上游也扩大。
- 扭曲后的粗网格边在与渲染器一致的偶列偏移坐标中形成二次 Bézier 中心线：相邻边的中点为端点，粗节点为控制点而非必须经过的硬拐点；真正的源头与入海终点保持原位置。所有入流曲线在共同下游边的中点以相同切线方向接合，不需要选取某一条“主支流”。曲线在连续渲染平面以不超过 1/16 邻格间距的插值误差细分（每段最多 32 个线段），采样点不先取整到格心。
- 栅格化沿曲线弧长线性变宽的扫掠圆盘包络；每个候选格只对该曲线包络判定一次。半径可以是小数，但所有输出坐标都是完整六边格；临界格由唯一几何规则决定水陆，不随机选择。自定义亚格宽度额外保留沿曲线的连通数字中心线。程序化河道仍使用 sea/coastal 类型，每格保持完整单一地形，并继续复用现有水面、岸线、通行与打包语义，不扩展 PackedWorldChunk。
- 程序化随机湖泊已经删除；手工 `lake` 和 `river` modifier 的编辑、渲染与持久化能力继续保留。

无限和有界世界以 128×128 页建立有上限的 LRU 河网位掩码；每页只枚举最大路径长度内可能流入该页的河源，搜索边距包括扭曲与最大河口半径，所以相邻页和不同请求范围结果一致。一次范围枚举会缓存排水节点及其唯一下游边，支流汇合后复用后缀采样；按规范化节点聚合流量、到海距离和宽度，每条共享下游边只栅格化一次。环绕世界一次建立规范化全域位掩码，河源只枚举一次，避免跨边副本重复追踪。

### 6.7 水体作者参数（v19）

`DEFAULT_WORLD_WATER_STYLE` 使用作者确认的截图参数；公开的 `WORLD_WATER_STYLE_RANGES` 是演示面板与 API 校验的唯一区间来源。所有字段必填；默认参数只在未传入整份配置时使用，不为缺失字段兼容旧描述符。

| 参数 / 字段 | 下限 | 默认 | 上限 | 面板步进 |
|---|---:|---:|---:|---:|
| 海域噪声频率 `oceanScale` | 0.7 | 1.4 | 2.8 | 0.05 |
| 海域覆盖阈值 `oceanLevel` | 0.32 | 0.46 | 0.60 | 0.005 |
| 河源区域间距 `riverSourceCellSize` | 8 | 16 | 32 | 1 |
| 每区河源数量 `riverSourcesPerCell` | 1 | 4 | 8 | 1 |
| 河流长度（基准%）`riverLength` | 10 | 100 | 300 | 5 |
| 河道弯曲频率 `riverWarpScale` | 0.02 | 0.08 | 0.12 | 0.001 |
| 河道弯曲幅度 `riverWarpAmplitude` | 0 | 3.75 | 3.90 | 0.05 |
| 支流半径 `riverBaseRadius` | 0.5 | 1.75 | 2.75 | 0.05 |
| 主河半径 `riverHighFlowRadius` | 3 | 4 | 6 | 0.05 |
| 主河流量阈值 `riverHighFlowThreshold` | 2 | 24 | 48 | 1 |

- `oceanLevel` 是海域场的分类阈值，不保证最终水体面积恰好为 46%。海域基础频率为 open 0.0035 / toroidal 0.006，统一乘以 `oceanScale`；基础 profile 与默认作者 profile 必须相同，不能遗漏或重复缩放。
- `riverLength` 表示相对于种子河源锚点到海弧长的目标倍率。100% 为基准，50% 缩短下游后缀，200%/300% 沿真实入流图向更上游移动河源。范围 10–300，步进 5；分母不再是整张河网最远端到海距离，因此超过 100 确实能增加新河段。它不保证每条河都增长到目标倍率：分水岭、山雪、世界边界或节点上限会使具体流域提前饱和。百分比也不等于河道面积比例。
- 所有倍率先建立相同的完整上游支路、唯一曲线、到海弧长和流量半径，再把 `anchor.distanceToSea × riverLength / 100` 的目标预算赋给扩展河头。预算按拓扑序传向下游，汇合处取最大值，每条共享边仍只栅格化一次。截取点位于细分曲线内部，半径沿用原宽度曲线。因此同一世界调长只增加上游河段，不移除已有水格或改变下游河口。源头基准用完整曲线在锚点对应河段起点的海距度量，不在粗节点硬切。
- 删除 `upstreamExtensionSteps` 和旧 `courseLengthRatio`，不保留兼容路径。倍率不改变 72 节点追踪上限或源区搜索半径。主河上限 6 对应最大河口半径 9.6，分页几何边距继续覆盖该上限。截图的九项默认值不变；100% 不再等于 v18 完整河网，生成器与描述符身份随语义显式升级。
- 支流和主河的合法区间不交叠，避免用户单独拖动滑块产生支流不小于主河的无效组合。幅度上限 3.90 保留小于半个粗步长的结构约束；不能围绕 3.75 对称扩展到 7.5。
- 整数参数校验步进；小数参数只校验有限数和范围，面板步进不作为 API 浮点量化规则。调整和重置都重新生成世界，并把包括长度在内的完整配置用于 Worker、缓存、存档和导航身份。

## 7. 世界拓扑与 512×512

三种拓扑共享风格，但边界条件不同：

### 7.1 无限世界

- 使用全局逻辑坐标。
- 没有隐式中心和边缘。
- 正负坐标规则相同。
- 所有计算必须有固定上界。

### 7.2 环绕世界

- 所有连续场必须以 width/height 为周期。
- 海域、河道、植被、地表高度和 Shader 相位都要跨四边与四角连续。
- width 继续满足六边格环绕约束。
- 512×512 是主要质量样本，但不是算法常量。

### 7.3 有界世界

- 保留边缘衰减，形成完整大陆。
- generateWorld() 必须与同域解析器的生成格子一致。
- 普通 StaticWorldSource 不猜种子，继续使用中性地表。
- 只有出现真实需求时，才设计带版本身份的有界流式世界源。

### 7.4 产品选择

| 需要 | 选择 |
|---|---|
| 512×512、四边无缝、支持流式加载 | ToroidalWorldSource |
| 有明确海洋边界或不可越界边缘 | 当前使用有界一次性生成；以后按真实需求增加有界流式世界源 |
| 没有世界边缘 | ProceduralWorldSource；512×512 只是观察窗口 |

有界世界不是无限世界的精确裁剪，环绕世界也不是把无限世界两边强行粘接。不能同时混用三种边界语义。

## 8. 区块与流式加载

### 8.1 保持不变

- WorldSource 仍是存储和输入输出边界。
- 源区块仍是生成、传输和驻留单位。
- 渲染块固定为 12×12。
- WorldStreamer 只表达镜头需求。
- ChunkResidencyCoordinator 继续管理租约。
- 一次 loadWorld() 仍拥有唯一世界源和生命周期。

### 8.2 必须保持的结果

同一描述符和坐标在以下变化下必须得到相同核心格子：

- 合法的源区块大小变化。
- 正序、逆序、随机和并发请求。
- 单工作线程与多工作线程。
- 缓存命中或未命中。
- 相邻区块是否已经驻留。
- 镜头位置变化。
- 卸载后重新加载。

一圈输出邻域继续满足最终渲染需要。生成器内部可以为计算邻域格子建立更大的短命窗口，但不能要求自定义世界源偷偷提供更多数据。

### 8.3 暂不扩充 PackedWorldChunk

v1 不立即增加 relief 或 biome 附加字段，原因：

- 当前结果可由描述符和解析器重建。
- 先统一规则，避免过早固定量化格式。
- 保持现有 16 位格子数据和缓存格式。

只有出现以下证据才升级区块格式：

- 主线程重复采样成为明确性能热点。
- 解析器开始依赖不能按坐标重建的区域结果。
- 远程世界源需要传递权威高度。
- 导航或存档必须保存量化地表高度。

## 9. 渲染与贴地

### 9.1 TerrainMesh

TerrainMesh 不再拥有山地阈值和高度公式，只负责：

- 读取生效格子与六邻居高度。
- 构造共享顶角。
- 生成渲染属性。
- 管理 LOD、材质、迷雾和资源生命周期。

每次渲染块构建使用自己的短命采样窗口。区块卸载、地形编辑或 WebGL 上下文恢复后，从当前 MapInfo 和解析器重建。

### 9.2 顶点属性限制

当前 Terrain 已使用 15 个顶点属性位置，不能随意增加字段。

阶段 2 已把 style 从 vec3 扩为 vec4，w 分量保存生效 relief；岸线、水体、湖泊和地图外空位使用负值哨兵，CPU 与 Shader 据此得到相同的共享顶角归零规则。这个改动没有增加属性位置。

阶段 4 完成后的打包状态：

- style 扩为 vec4 已完成。
- fogState 已扩为 vec4：x 存迷雾，y/z/w 存 dry/cold/alpine，temperate 由总和推导。
- landform attribute 继续保存四种生成器调试字段；顶点阶段把最终有效 elevation 写入 vLandform.x，并保留 y/z/w 的 ridge/valley/roughness，已删除重复的 vElevation varying。
- full/fast 材质都使用同一组连续 biome 权重做色调、冷暖和高山去饱和混合，不增加 atlas 采样；每个 fragment 仍只有一次地形纹理读取。

硬门槛：

- 不超过最低支持上下文的 MAX_VERTEX_ATTRIBS 和 varying 上限。
- full 与 fast 使用相同属性布局。
- 新增属性字节进入几何资源预算。
- 不先占满能力再假设用户 GPU 更强。

### 9.3 地表高度查询

阶段 3 已把包内 WorldSurfaceAnchor / WorldSurfaceView 升级为 generator v4 的连续宏观地表，并完成以下对象的贴地迁移：

以下对象统一使用 WorldSurfaceAnchor：

- 城市模型和标签。
- Unit 移动。
- 选择器和悬停标记。
- 路线。
- 草和树。
- 注册的自定义渲染层。
- 按格子聚焦的镜头目标。

草和树按每个实例的 XZ 查询高度，不能整格共用中心 Y。

Unit 沿路径移动时按当前位置调用 getWorldHeight()，但朝向只使用 XZ 平面的偏航角，不随小坡频繁俯仰。

路线和标记在地表上增加统一小偏移，避免闪烁。

HexMap.add(Object3D) 无法知道对象属于哪个格子，因此不承诺自动贴地。调用者必须主动查询高度，或使用 WorldRenderLayer。

### 9.4 植被工作线程

工作线程只生成：

- 所属格子。
- 局部 XZ。
- 缩放和朝向。
- 树种与密度布局。

最终 Y 在主线程挂载时由 WorldSurfaceView 批量计算。这样 mountainHeight 可以实时修改，也不会把旧显示尺度写进工作线程结果或缓存。

### 9.5 动态高度刷新

WorldRenderLayerHost 增加可选 surface?: WorldSurfaceAnchor。所有内置 host 都提供它；旧自定义层可以回退到 Y=0。

使用 surface 的自定义层必须实现可选回调：

~~~ts
surfaceChanged?(host: WorldRenderLayerHost): void | Promise<void>;
~~~

普通消费者通过 surfacechange 事件取得 { revision, surface }。

更新顺序：

1. 增加地表显示版本。
2. 更新 WorldSurfaceView 和 Terrain。
3. 重定位轻量对象。
4. 暂时隐藏仍使用旧版本的植被和注册层对象。
5. 异步重建完成并确认世界生命周期、地表版本仍有效。
6. 显示新对象并发送 surfacechange。

旧异步结果只能释放，不能覆盖新地表。

### 9.6 裁剪与迷雾

- Terrain 的水面、河床和山体 Y 边界从水深、波幅与地表上限推导；阶段 4 将山体 Shader 细节上界同步收紧到 1.015 倍。
- 草和树的 Y 边界已经由统一地表上下限与对象余量推导；自定义层继续通过自己的 WorldChunkMetadata 声明边界。
- 地形、水、草、树和自定义层的 Y 边界必须由统一地表上限与对象余量推导。
- 不再依赖固定的 -size × 2 到 size × 3 经验范围。
- 迷雾可以隐藏地形轮廓，但 WorldSurfaceAnchor 始终返回真实高度。
- 不能把未探索格子的高度改为 0，否则对象恢复显示时会跳动。

## 10. 编辑、版本和恢复

### 10.1 编辑刷新

- 仅 unit 字段变化：不重建地表。
- city 变化：刷新城市与植被抑制。
- terrain、modifiers、treeModel 和 river 变化：刷新目标格子与六邻居。
- 改变通行性或移动消耗：使对应导航摘要失效。
- mountainHeight 等显示尺度变化：只增加渲染地表版本，不改变导航版本。
- 存档恢复期间继续拒绝编辑、清空和新快照。

### 10.2 版本职责

| 变化 | 必须更新 |
|---|---|
| 格子分类、修饰、树种或 CPU 地表语义 | WORLD_GENERATOR_VERSION 与对应基准校验和 |
| 区块字段、邻域或传输布局 | WORLD_CHUNK_FORMAT_VERSION |
| 工作线程请求、响应或转移对象 | WORLD_WORKER_PROTOCOL_VERSION |
| WorldDescriptor 字段或含义 | WORLD_DESCRIPTOR_FORMAT_VERSION |
| 只有 Shader 颜色和光照变化 | 不升级生成器；执行视觉回归 |

生成器版本唯一对应一个冻结基础配置；每个世界的规范化水体参数作为独立身份字段保存，不再增加一套与生成器总是一起增长的风格版本。

### 10.3 唯一世界指纹

继续使用现有规范序列化：

~~~ts
const fingerprint = serializeWorldDescriptor(descriptor);
~~~

该字符串是唯一身份来源：

- 缓存键使用 [fingerprint, chunkX, chunkY]。
- 默认 worldId 使用 fingerprint 或其稳定哈希。
- 导航 terrainRevision 使用 fingerprint 或其稳定哈希。
- 存档继续保存并严格比较完整描述符。
- 水体参数变化会得到新的指纹，因此 Worker 解析器、区块缓存、增量存档和导航版本不会与旧参数混用。

不能在不同调用点分别拼接字段。

工作线程请求和响应必须包含并校验生成器版本与区块身份。校验完成前不能写缓存或安装到稀疏地图。

### 10.4 一次迁移

迁移与后续身份修订已完成：

1. 提取 WorldSurfaceResolver。
2. 建立 WorldSurfaceView 并复现现有视觉。
3. 一次性升级到 generator v4，加入连续地表、气候、森林/湖泊斑块和全部贴地消费者。
4. 阶段 5 复核发现低适宜度森林仍被独立随机值打散；generator v5 改为连续密度阈值加有限边缘抖动，不改变宏观地貌、气候、湖泊或地表高度。
5. generator v6 把永久积雪限制到气候雪线以上，并让生成雪地保留 hill relief；低温低地继续使用 tundra，山体峰顶由连续 Shader 雪线覆盖。同时渲染法线改为共享顶角坡度，常驻六边格线默认关闭。
6. generator v12 删除程序化随机湖泊和最终 elevation 海陆切分，改为独立低频海域与确定性粗网格排水河道；小地图通过范围枚举一次栅格化河道。
7. generator v13 保持海域不变，以低频连续扭曲打散河道粗网格方向，把每个源区候选从 3 个增至 4 个，并用范围内下游节点缓存抵消额外支流成本。
8. descriptor v2 / Worker protocol v4 将受限的水体生成参数纳入世界身份，并让一次性生成、区块 Worker、概览 Worker 和渲染侧解析器使用同一份参数。
9. generator v17 对齐新的水体默认值并开放 `riverLength`；新增必填身份字段，因此 descriptor 升至 v3、Worker protocol 升至 v5。旧版本和缺少长度的配置明确拒绝，不迁移或降级。
10. generator v18 将 `riverLength` 的含义由上游搜索格数改成实际河长百分比；descriptor 升至 v4、Worker protocol 升至 v6，拒绝旧含义，不能把旧的 24 格默认为 24%。默认 100% 的格子基线保持不变。
11. generator v19 改成河源锚点相对倍率，探索最长真实上游支路，范围 10–300%；descriptor 升至 v5、Worker protocol 升至 v7，不把 v18 的“完整河网百分比”解释为新基准倍率。

当前固定格子回归校验和分别为：无限区块 `31edd4fd`、环绕区块 `5c34968c`、有界世界 `99fb0dc5`；规范量化地表校验和为 `ce340641`，森林/水体区域样本为 `62718e8f`。一次性生成、区块生成和工作线程共享同一解析器；工作线程按完整描述符指纹复用解析器。

v19 额外锁定 `new-world` 的 (-256, -256)、512×512 河道集合：坐标字符串排序、用分号连接后计算 FNV-1a，100% 为 `ed375a16`，300% 为 `65ae1140`。已有小范围格子校验和未变，不代表整个河网未变。

阶段 3 升级过生成器版本与 Worker 协议；阶段 5 和 generator v6 修订都只升级生成器版本。小地图概览后来增加了只读 RGBA 栅格请求，因此 Worker 协议升级为 v3。水体参数加入请求和描述符后，Worker 协议升级为 v4、descriptor 升级为 v2；长度字段加入后分别为 v5 / v3，保留比例修正后为 v6 / v4，锚点倍率与上游增长修正后为 v7 / v5。PackedWorldChunk 仍为 v1，因为紧凑区块布局没有变化。

旧生成器世界继续按严格描述符不匹配拒绝。自定义固定 worldId 的调用者必须主动迁移或清理旧地形编辑。

## 11. 性能边界

- 每个世界生命周期只创建一次解析器。
- 每个区块构建只创建一个有界采样窗口。
- 禁止按世界坐标建立无界 Map 缓存。
- 无限/有界河网页固定为 128×128，LRU 上限 16 页；环绕世界只保存一个规范化全域位掩码。
- 小地图先按目标范围枚举粗网格路径，再栅格化到像素覆盖；禁止对 2048² 世界逐格解析或从每个像素重复追踪河道。
- 同一范围内的河源必须复用排水节点与下游选择缓存；缓存随单次范围生成释放，不进入常驻状态。
- 只有实际分析证明对象分配成为热点时，才增加 sampleInto() 等内部快速路径。
- 常驻属性、模型和以后可能增加的附加字段进入资源账户。
- 临时采样窗口不计作常驻资源，但必须按区块尺寸有明确上限，并在构建后释放。
- 调试模式不能每帧重采样全部驻留世界。
- 自适应画质只能减少实例和细节，不能改变权威格子、气候和植被语义。
- 重点测量首次可见区块、稳定区块生成、植被准备、挂载时间和 GPU 属性字节。
- 额外记录 2048×2048 世界到 256×256 小地图的生成耗时；v13 本机七次直接调用中位数约 0.237 秒，河流像素从 v12 的 959 增至 1,527，属于一次性后台任务与密度观测，不作为跨机器绝对门槛。

## 12. 测试与视觉验收

### 12.1 确定性测试

必须覆盖：

1. 同种子、拓扑和坐标结果一致；不同种子有差异。
2. 一次性生成与环绕流式生成的核心格子一致。
3. 多种合法源区块大小结果一致。
4. 正序、逆序、随机和并发请求校验和一致。
5. 单工作线程与多工作线程一致。
6. 相邻区块核心和邻域双向一致。
7. 环绕世界四边和四角的字段、格子和 relief 一致。
8. 无限世界负坐标与安全整数边界正确。
9. 风格配置拒绝 NaN、Infinity、负尺度和乱序阈值。
10. 修改风格配置或生成算法时必须同步升级生成器版本。
11. mountain、hill、plain、水体以及手工 lake/river 编辑后的高度范围正确，清除编辑可恢复。
12. 静态世界源使用中性高度，不猜种子。
13. 工作线程校验生成器和区块身份。
14. 固定坐标集具有格子校验和与地表校验和。
15. full/fast Shader 在最低属性能力下都能链接。
16. 河道范围枚举与分页格子解析完全一致；不同范围的重叠区域一致，环绕平移保持同一身份。
17. 每个生成河道连通分量必须到达宏观水体或观察窗口边缘；2048 范围的低分辨率海域组件数与孤立点受门禁约束。

地表校验和至少覆盖 relief、biome 权重、植被密度和 ocean 场的规范量化结果。

### 12.2 几何与流式测试

固定一条跨越以下边界的山系：

- 两个源区块。
- 多个 12×12 渲染块。
- near、middle、far LOD。
- 环绕边界。
- 卸载和重载。
- 无限世界浮动原点重定位。

直接比较 CPU 共享顶角和边缘属性，不能只依靠截图。

浏览器验收还要覆盖：

- 地形稀疏编辑。
- mountainHeight 动态修改。
- 城市、单位、路线、草木和标记贴地。
- 迷雾三种状态切换。
- WebGL 上下文丢失与恢复。
- Y 边界擦边裁剪。
- 属性、纹理和资源计数不增长。

### 12.3 世界样本图库

首版固定：

- 4 个有界世界种子。
- 6 个 512×512 环绕世界种子。
- 4 个无限世界种子，并包含正负坐标窗口。
- 少量接近全水、全陆的极端种子。

每个样本保存固定远景、中景、近景和调试视图。完整截图作为持续集成产物，只有少量跨 GPU 稳定的图进入仓库。

记录以下指标：

- 陆地、水体、山地和气候比例。
- 山地连通块数量、孤立山格比例和长度分布。
- 森林邻接率和斑块大小。
- 水体连通块、主水体占比和孤立水格比例。
- 海岸线长度。
- 拓扑接缝错误数。
- 首区块、稳定区块和植被准备耗时。

指标先建立基线，再设宽松范围，不能为了迎合所谓“真实比例”反向扭曲世界。

连通性统计必须使用六邻接并尊重拓扑：环绕世界跨边合并，有界世界不跨边，无限世界标记触及观察窗口边界的截断组件。

固定语料包含 4 个有界世界、6 个 512×512 环绕世界、4 个无限世界种子的正负坐标窗口、2 个水陆极端样本和 2 个最小尺寸样本。`npm run review:world-style:metrics` 执行拓扑与区域统计门禁；浏览器图库只在明确需要人工视觉复核时单独运行，截图和逐样本 JSON 不提交到仓库。

v19 默认 100% 锚点河长的水文观测基线如下；门禁使用更宽的范围，避免为了追逐固定比例反向扭曲风格。山地仍执行原有构成门禁，未改变山地生成规则：

| 语料 | 水体比例 | 森林比例 | 森林邻接率 | 水体主组件占比 | 接缝错误 |
|---|---:|---:|---:|---:|---:|
| 4 个有界世界 | 0.486–0.689 | 0.0026–0.0761 | 0.875–0.994 | 1.000 | 0 |
| 6 个 512 环绕世界 | 0.218–0.435 | 0.042–0.0704 | 0.987–0.992 | 0.568–1.000 | 0 |
| 8 个无限世界局部窗口 | 0–1.000 | 0–0.146 | 0.986–1.000（有森林样本） | 0.872–1.000（有水样本） | 0 |

有界样本 a 的森林比例从旧基线增长到 0.076 是海陆覆盖变化暴露了原森林区域；只调整有界森林比例上限至 0.09，邻接/孤立点门禁保持不变。极端样本重新固定为 `gallery-torus-38` 的 64 环绕域（约 0.0066 水体）与 `gallery-open-ocean` 的 (256, 0) 无限窗口（全水），保留 <5% / >90% 门禁，不能保留已不再极端的旧种子再放宽条件。

v19 默认锚点位置改变了上游水陆覆盖，512 环绕样本 f 的森林占比为 0.070377，略超旧 0.07 上限；森林生成代码与连续地表校验和均未变化。仅将该组森林比例上限调整为 0.075，保留全部森林邻接、孤立点、山地和水体连通门禁。

极端样本覆盖近全水和近全陆结果。无限世界局部窗口允许落在大陆腹地或外海，不再对每个 144×144 窗口强制平均水体比例；另用 2048 范围的粗采样门禁约束宏观水体碎片化。性能 JSON 同时记录首次可见、完全稳定、Worker 地形/植被任务、GPU/CPU 帧耗时和资源计数；不同 GPU 不设截图像素和绝对耗时硬阈值。

### 12.4 正常验收

按 [testing.md](./testing.md) 执行：

- 单测。
- 类型检查。
- 构建。
- 浏览器端到端测试。
- 涉及工作线程或渲染生命周期的改动才要求执行对应 soak；本次只更改解析器内部与已有概览任务。
- 性能基准。
- 固定指标语料；需要人工视觉验收时只运行与改动直接相关的场景。

## 13. 实施顺序

### 阶段 0：建立基线（已完成）

- 固定种子、拓扑和镜头。
- 记录当前格子校验和、地表样本、截图和性能。
- 增加跨区块、LOD 和环绕边界的山系场景。
- 记录当前 15 个属性位置和最低测试设备能力。
- 不改变生成结果。

### 阶段 1：提取统一解析器（已完成）

- 新建 WorldSurfaceResolver。
- 合并一次性生成与区块生成的重复规则。
- 默认配置完整复现 generator v3。
- 格子校验和不得变化。

### 阶段 2：建立生效地表（已完成）

- 新建 WorldSurfaceView 和短命 WorldSurfaceWindow。
- TerrainMesh 通过视图复现当前山体高度。
- 固定编辑优先级、静态回退、岸线和 Y 边界测试。
- 暂不改变默认世界。

### 阶段 3：一次升级世界风格（已完成）

- 绑定新的冻结风格配置和下一个生成器版本。
- 统一世界指纹、缓存键、默认 worldId 和导航版本。
- 工作线程请求和响应加入生成器身份。
- 完成平地、谷地、丘陵、山麓和山体高度。
- 迁移城市、单位、路线、标记、草木和自定义层贴地。
- 完成连续气候、森林斑块和湖泊斑块。
- 只发布一次生成器迁移和一组最终校验和。

### 阶段 4：渲染与性能收敛（已完成）

- Shader 山体细节已限制为 CPU 宏观地表的 ±1.5%，并同步裁剪上界。
- biome 权重已打包进 fogState 的空余分量，Terrain 仍使用 15 个 attribute location。
- full/fast 共用宏观顶点表面和属性布局，材质混合不增加纹理读取。
- 山体光照使用共享顶角坡度；公共边法线不再由各实例分别有限差分。
- 常驻六边格线默认关闭，仅在调用者明确启用时绘制。
- 区块、LOD、环绕边界、长距离浮动原点、迷雾局部更新和十轮 WebGL 上下文恢复已通过浏览器验收。
- 单测、真实 WebGL 和性能基准没有显示主线程重采样热点，因此不升级 PackedWorldChunk，也不增加 sampleInto() 快速路径。

### 阶段 5：样本图库复核（已完成）

- 固定语料、六邻接连通统计、宽松比例门禁和四视图 Playwright 图库已经建立。
- 最小合法尺寸、512 环绕世界、无限世界正负坐标、近全水和近全陆极端样本均无拓扑接缝错误。
- 复核确认并修复了椒盐森林：generator v5 使用连续密度阈值和有限边缘抖动，森林邻接率提升到 0.891–1.000，常规大样本孤立格低于 0.11。
- 未加入侵蚀、全局地图集、新公共参数或河流；临海山体、山脉连通和湖区结果不需要修改。

### 阶段 6：决定是否加入自动河流（原结论已由阶段 8 替代）

<!-- optimization-gate:automatic-river-generation -->

- 阶段 6 当时没有足够缺陷证据，因此延后自动河流并保留手工 `river` modifier。
- 2026-09-04 的 2048×2048 世界概览直接暴露出最终 elevation 切水造成的高频碎片化；随后两轮逐格/采样水体试验又稳定暴露出河道单格、形态不自然和小地图生成显著变慢。
- 用户明确要求优化算法后，门禁由 [粗网格排水水体决策](./decisions/coarse-drainage-water-network.md) 重新开启并在阶段 8 实施。

### 阶段 7：冻结世界风格 v1（已完成）

- 正常测试、类型检查、构建、真实浏览器 E2E、500 次世界替换 soak、性能回归门槛和完整样本图库均已通过。
- 该阶段的 generator v6 校验和是历史基线；阶段 8 已按版本职责替换为当前水文基线。
- 后续玩法只消费地表接口，不把领域状态塞进 HexMap 或生成器；格子语义变化按版本职责升级生成器，纯渲染变化不改生成器身份。

### 阶段 8：修订海域与河网（已完成）

- 删除程序化随机湖泊以及由最终 elevation 直接切分的碎水体；保留手工 lake/river 编辑能力。
- 新增独立低频 ocean 场，地形 detail 只影响陆地起伏，不再改变海陆掩码。
- 新增 WorldWaterSampler，在 8 格间距的粗六边网格上按稳定河源、严格下坡、到海保留和汇流计数生成 3–5 格宽河道。
- 无限/有界世界使用有上限的分页掩码；环绕世界使用一次性规范化掩码；跨页、跨请求范围和跨环绕边界均由确定性测试锁定。
- 小地图只枚举一次目标范围河道并映射到像素覆盖，不构造 2048² 格子；2048×2048 到 256×256 的本机专项观测为直接调用约 0.32 秒、浏览器 Worker 中位数约 0.40 秒。
- 固定指标语料、单测、类型检查、构建、边界和性能门禁必须与本阶段一起通过；本阶段不运行无关的完整截图图库。

### 阶段 9：河道自然度与小地图拖动复核（已完成）

- 用户确认宏观海域和总体水量可用，但主世界河道仍暴露粗网格的横斜折线，小支流偏少；海域参数保持冻结。
- 排水图仍以 8 格为步长，世界坐标嵌入改为低频连续扭曲；这只改变河道位置和局部方向，不引入白噪声随机游走或全图侵蚀。
- 每个源区候选增至 4 个，高流量阈值同步从 6 调到 8，避免支流增加后主河普遍膨胀；共享下游节点在单次范围内缓存。
- 小地图预取从一圈增至两圈；右键拖动超过当前分页跨度的四分之一时立即重算需求，松开时最终对齐，阈值内只合成缓存页。
- 确定性、范围重叠、分页/环绕一致性、河流像素密度、小地图连续拖动无黑框、类型检查和构建必须通过；不运行无关的完整截图图库。

### 阶段 10：整格河岸栅格修订（已完成）

- 世界生成与玩法继续保持严格分类：一个六边格只能整体为水或整体为陆地；现有窄岸边材质过渡不改变格子的地形归属。
- 修正河网轴坐标原先使用奇列偏移公式、而渲染与邻接使用偶列偏移公式的不一致；中心线每一步现在都是真实可见六邻格。
- 河宽由“沿离散中心线重复叠加六边形图距离圆盘”改为“格心到理想世界空间线段的距离”；临界格以唯一几何规则决定水陆，不用随机数制造岸边噪点。
- 保留中心线连通、汇流宽度、分页/范围/环绕身份和现有 sea/coastal、通行、打包语义；生成器身份升级到 v14。

### 阶段 11：河源延伸与渐宽河口（已完成）

- 默认弯曲频率从 0.025 增至 0.03，幅度从 2.5 增至 3.25；海域场与海平面保持不变。
- 支流/主河半径由 1/2 改为 1.25/2.75；水体作者参数允许小数半径，演示面板步进为 0.05。不引入额外世界风格字段，内部延伸/河口规则由生成器 v15 锁定。
- 河源最多向上游延伸 3 步，汇流宽度连续化，河口最后 24 格渐宽至 1.6 倍。最大追踪长度与候选数量不变，不预计算全域水文。
- 扫掠变宽圆盘包络与下游边去重替换原来的定宽/重复栅格化路径；移除旧函数，不保留平行实现。
- 回归覆盖变宽方向对称性、端点圆盘包含、零长度与亚格宽度连通、受控坡面上的上游延伸/河口局部增宽，以及范围、分页、环绕一致性。原本的海域连通门禁保持不变；概览河道像素范围随这次有意增加的面积更新。

### 阶段 12：消除粗网格折线感（已完成）

- 仅增加噪声扭曲会得到更多折线；v16 把直连粗节点改为圆化拐点的二次 Bézier 曲线，再进行整格水陆分类，不修改六边形渲染几何，也不引入半水半陆格。
- 同一规范化节点只建立一条下游曲线，入流是否存在由完整保留的河网决定；只有真正的河源保留起点，内部节点不额外长出“到控制点”的直线短枝。海口展宽使用新曲线的累计弧长。
- 曲线控制点在原路径凸包内，不制造向外的过冲或回钩。分页搜索额外保留一格粗步长的几何边距；环绕汇流按当前展开坐标还原下游向量，防止曲线横穿整张地图。
- 回归直接检查原硬拐点被圆化后不再强制归水、两条支流与下游共端点/同切向，并重跑整格连通、分页重叠、宽河口环绕与河流视角对照。此修订的视觉复核聚焦河道拐弯/汇流/河口；完整 22 组地形图库已在此前 v15 修订通过，不把无关森林/山体全图库反复生成作为河道迭代的前置条件。

### 阶段 13：水体默认值与长度作者参数（已完成）

以下记录 v17 的历史方案；长度含义与 UI 层级由阶段 14 修正，当前配置以 6.7 为准。

- 截图中的九项参数成为新默认，所有作者参数的范围由公共冻结元数据共享；中英文面板增加河流延伸长度，调整与重置均触发完整配置生成。
- 长度接入已有真实入流边延伸，不新增随机路径、固定长度硬截断或无效果的追踪上限滑块。维持整格水陆分类、圆化曲线与渐宽河口。
- 修正幅度隐式截断与非 1 默认海域倍频下的基础 profile 一致性。升级生成器、描述符、Worker 协议，更新受影响的校验和。
- 回归覆盖所有字段上下界、非法数和缺失字段、长度身份隔离、0/24/96 格延伸效果及分页一致性、最大河宽/长度的真实跨边河道、浏览器调整/重置和固定指标语料。
- 新增控件后，720p 下的重置按钮曾被折叠小地图遮挡：`body > .dg.ac` 以明确的选择器优先级抵抗 dat.gui 后注入的层级样式，保持控件面板 z=10、折叠小地图 z=9、展开地图 z=19；不绕过真实点击测试。
- 旧植被基准坐标在新默认下成为海洋；改用有草木的固定 `new-world` (0, 0) 区块，并明确拒绝空草/空树基准，防止用空负载报告性能通过。此轮不修改树木几何或渲染器，不重复完整地形截图图库。

### 阶段 14：实际河长与小地图调参刷新（已完成）

以下长度比例记录 v18 历史方案，由阶段 15 替代；小地图生命周期与层级继续生效。

- 复现旧滑块饱和：`new-world` 的 (-256, -256)、512×512 窗口在旧长度 24/48/96 下均为 1,443 个河道格。原因是符合条件的上游已耗尽，不是滑块没写入参数。
- 改为按完整路线实际弧长截取下游后缀，同窗口的 10/25/50/75/100% 分别为 173/415/729/1,091/1,443 格；每档集合包含前一档。受控海岸与真实种子同时验证河口保持、连通、宽度一致、分页重叠与环绕身份，不能只用理想坡面证明作者参数有效。
- 展开小地图是非模态观察面板；控制面板 z=21、性能监控 z=20、展开地图 z=19、遮罩 z=18。监控保持不拦截指针，参数面板在地图展开时可点击与拖动。
- `loadstart` 只失效旧页、停止旧请求和悬停/缩放动画，保留展开视角；`load` 后按新世界边界约束原视角并重建可见页，不再调用会收起地图的 `clear()`。加载期间禁止帧循环对未发布的新世界发概览请求，旧世代结果仍被丢弃。
- 小地图页脚提供“刷新”，避免置顶监控遮住入口；手动调用 `refresh(true)` 失效页数据但不重置视角，生成中禁用按钮并显示加载状态。世界参数提交自动刷新；保持脏标记，不引入周期性全图重算。详细生命周期见 [render-streaming.md](./render-streaming.md#data-driven-world-overview-and-minimap)。
- 浏览器回归使用真实拖动：缩放/平移展开地图后改河长，检查页世代和像素变化、原视角保留，再手动刷新检查世代推进且像素相同。世界替换另执行 500 次 soak。

### 阶段 15：超过基准的真实上游增长（已完成）

- v18 只能缩短完整河网；把上限文字改成 300% 仍不会产生新河段。v19 删除旧三步贪心与完整河网比例，采用河源锚点、最长真实入流支路和锚点到海目标倍率。
- 同一 `new-world` (-256, -256)、512×512 范围在 10/50/100/150/200/250/300% 下分别为 146/641/1,289/1,463/1,525/1,595/1,648 个河道格；每档严格包含前一档，300% 超过 v18 完整河网的 1,443 格。源头/河口的受控回归确保不是单纯加宽；分叉夹具确保选择较长低势能支路而非较陡短死端，并检查山体阻断。
- 不承诺每个流域随每一档都增长：`river-length-review` 同范围在 150% 后已耗尽上游支路，保持不变是地形约束，不增加随机游走或强行穿山。
- 单次范围内的记忆化上游查询有节点深度界限，不添加常驻缓存或扩大分页搜索边距；高倍率仍复用相同完整图。整格水陆归属、曲线圆化、下游宽度、河口与小地图刷新生命周期不变。
- 浏览器用真实滑动从 100% 调至 200%，检查展开地图像素变化、观察视角保留与手动刷新稳定。回归覆盖全区间集合增长、分页/范围一致、300% 宽河口环绕和完整世界指标；不重复无关地形全图库。

## 14. 复核结论

### 14.1 是否有更优的整体方案

对当前目标，最合适的是：

~~~text
无状态生成字段
+ 区块内短命采样窗口
+ 合并编辑的运行时地表视图
~~~

它保留现有流式加载和六边格表现，也避免全世界预计算。

当前河网使用可重算的粗网格排水页，不需要全局地图集。只有玩法要求完整流域蓄水、河流编辑传播或权威远程水文时，才有理由引入持久化区域图。

### 14.2 剩余风险

| 风险 | 处理方式 |
|---|---|
| 岸线归零使临海山体像被切平 | 单独复核临海山地；必要时改为对称岸线衰减 |
| 512 世界好看但小世界重复明显 | 测最小尺寸和多个中间尺寸 |
| 低频海域使小型局部窗口全陆或全海 | 局部窗口不强制平均比例；用 2048 宏观样本与极端种子分别验收 |
| 粗网格河道出现分页断裂或缩放闪烁 | 对范围重叠、分页解析和环绕周期做逐格一致性测试 |
| 小地图重新出现逐格水文热点 | 概览只调用一次河道枚举；持续记录 2048→256 专项耗时 |
| 普通地形起伏影响六边格识别 | 严格限制平地和丘陵高度，保留清晰网格 |
| 静态 MapInfo 无法复现程序化精确高度 | 保持中性回退；有真实需求再加显式能力 |
| 主线程重算高度成为热点 | 先使用短命窗口，性能数据证明后再传附加字段 |
| 非 Chromium 平台出现浮点边界差异 | 扩大平台支持时，在新生成器中统一量化 |
| 自定义层使用高度但不处理 surfaceChanged | 文档和示例明确要求；以后主版本再考虑强制能力 |

结论：阶段 0–15 已完成。生成、身份、贴地、biome 材质语义、气候雪线、区域森林、宏观海域、圆化的粗网格排水河道、渐宽河口和整格河岸归属已经统一；河长支持超过基准的真实上游增长，并支持小地图原位调参刷新。不引入侵蚀、物理水循环或全局地图集。程序化水体与手工 lake/river 编辑能力并存。

## 15. 完成标准

世界风格 v1 只有同时满足以下条件才冻结：

1. 一次性生成和流式生成共享唯一解析器。
2. 风格配置与生成器版本一一对应。
3. 世界描述符、缓存、存档、默认 worldId 和导航版本使用同一指纹来源。
4. 多个种子的远、中、近景没有明显碎山、椒盐森林、噪声水点和尺度冲突。
5. 平地、谷地、丘陵、山麓和山体形成连续且克制的层次。
6. 气候、树种、植被密度和地形材质使用同一套空间语言。
7. 源区块、渲染块、LOD、环绕边界、卸载重载和浮动原点没有接缝。
8. 城市、单位、路线、标记、草木和注册层正确贴地。
9. 地形块不会因 Y 边界错误而提前裁剪。
10. 格子与地表校验和、版本变化、缓存隔离和旧存档拒绝规则明确。
11. 单测、类型检查、构建、端到端测试、适用 soak、性能基准和样本图库复核全部通过。
12. 没有为假设需求引入全局地图集、通用特征系统、连续地形重构或物理模拟。

上述门槛在 2026-08-29 首次通过；2026-08-31 完成积雪与山体接缝复核，2026-09-04 完成海域与河网修订。当前固定格子、连续地表和区域水体校验和见 10.4，固定样本构成与比例基线见 12.3。今后的生成语义改动必须显式升级生成器版本并同步校验和、缓存/存档身份和指标基线。

完成后，停止继续扩建世界生成基础设施，进入确定性模拟核心和第一批真实玩法内容。
